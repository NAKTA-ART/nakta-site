# build output
dist/
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment
.env
.env.production
.env.local

# macOS / editors
.DS_Store
.vscode/
.idea/

# Claude Code 로컬 설정(각자 PC 전용)
.claude/settings.local.json

# 디자인 원본 PSD (약 122MB) — git 에 넣으면 저장소가 무거워집니다.
# 버전 관리가 필요하면 Git LFS 를 쓰세요: git lfs track "*.psd"
design/*.psd
